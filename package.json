{
  "name": "@steemit/libcrypto",
  "version": "1.0.0",
  "description": "Common javascript cryptographic utilities. Based on sjcl.",
  "main": "lib/crypto.js",
  "license": "MIT",
  "scripts": {
    "prepublish": "npm run build",
    "clean": "rm -Rf lib",
    "fmt": "prettier --write '{src,scripts}/**/*.js' && eslint '{src,scripts}/**/*.js'",
    "build": "node ./scripts/build.js",
    "sjcl-test": "node ./scripts/test.js",
    "test": "npm run build && npm run sjcl-test && tape test/crypto.test.js"
  },
  "devDependencies": {
    "eslint": "^4.10.0",
    "prettier": "~1.8.2",
    "tape": "^4.8.0"
  }
}
